pipeline:
  name: Lab12Pipeline
  identifier: Lab12Pipeline
  projectIdentifier: bootcampciaysha
  orgIdentifier: default
  tags: {}
  properties:
    ci:
      codebase:
        connectorRef: account.FirstGitHubConnector
        repoName: harness-docs
        build: <+input>
  stages:
    - stage:
        name: TestStage
        identifier: TestStage
        description: ""
        type: CI
        spec:
          cloneCodebase: true
          caching:
            enabled: true
            override: true
          buildIntelligence:
            enabled: true
          infrastructure:
            type: KubernetesDirect
            spec:
              connectorRef: secondk8sconn
              namespace: default
              automountServiceAccountToken: true
              nodeSelector: {}
              os: Linux
          execution:
            steps:
              - step:
                  type: Run
                  name: Run_1
                  identifier: Run_1
                  spec:
                    connectorRef: account.HarnessDocker
                    image: nginx:1.29
                    shell: Sh
                    command: echo "hello world"
